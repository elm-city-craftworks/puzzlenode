.puzzle-info
  .left
    This puzzle was contributed by
    = link_to @puzzle.created_by, @puzzle.created_by_url, :target => "_blank"
    and published on
    = @puzzle.released_on.strftime("%B %e, %Y")

  .right
    = render :partial => "action_button", :locals => { :puzzle => @puzzle }
