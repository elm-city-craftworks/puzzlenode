!!! 5
%html
  %head
    %meta{:charset => "utf-8"}
    %title #{yield(:title).blank? ? controller_name.humanize : yield(:title)} - PuzzleNode
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1"}
    = stylesheet_link_tag 'application'
    = csrf_meta_tag

    = yield(:header_bottom)

  %body{class: body_css}
    = render "shared/flash", flash: flash
    %header
      = render "shared/nav"
      = render "shared/admin_nav" if current_user.try(:admin)
    #content= yield
    -# #footer= render "shared/footer"
    = javascript_include_tag 'application', :debug => Rails.env.development?
